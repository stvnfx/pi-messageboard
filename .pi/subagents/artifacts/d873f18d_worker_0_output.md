Published successfully. `pi-messageboard@0.1.1` live on npm.

Skipped: git commit/tag (version bump is uncommitted — commit locally if desired).
Note: test files shipped in tarball. Add `"exclude"` or remove from src if unintended.